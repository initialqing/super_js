let p = new Promise((resolve, reject)=>{
    // pending
    /**
     * 改变状态为成功
     */
    // resolve()
    /**
     * 改变状态为失败
     */
    // reject()
    // throw 'error'
    /**
     * 中断promise链
     *
     */
    return new Promise(()=>{})
})
